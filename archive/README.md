# Archive

Work kept in the repository but no longer shipped in the site or the apps.

- `textbook.html`, `textbook-card.jpg`: the site's own three-chapter Digital Textbook (methods, the
  neuron as a battery, the synapse in whole numbers), with its cited claims and citation-gap view,
  as it stood when the OpenStax book replaced it in September 2026. It was a single self-contained
  page and expects the site's `assets/` and `fonts/` beside it, so to read it again copy it back
  into `site/`.
